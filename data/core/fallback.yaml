version: "2.0"
rules:
  - rule:
    steps:
      - intent: nlu_fallback
      - action: two_stage_fallback
      - active_loop: two_stage_fallback
  - rule:
    steps:
      - intent: out_of_scope
      - action: respond_out_of_scope